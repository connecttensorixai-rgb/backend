import nodemailer from 'nodemailer'; const sendEmail = async (options) => { const transporter = nodemailer.createTransport({ service: 'gmail', auth: { type: 'OAuth2', user: process.env.GMAIL_USER, clientId: process.env.GMAIL_CLIENT_ID, clientSecret: process.env.GMAIL_CLIENT_SECRET, refreshToken: process.env.GMAIL_REFRESH_TOKEN, }, }); console.log(`[EmailUtils] Initializing Gmail OAuth2 transport for: ${process.env.GMAIL_USER}`); const mailOptions = { from: `"TensorixAI Notifications" <${process.env.GMAIL_USER}>`, to: options.email, replyTo: options.replyTo, subject: options.subject, text: options.message, html: options.html || options.message.replace(/\n/g, '<br>'), }; console.log(`[EmailUtils] Dispatching email to: ${options.email}`); const info = await transporter.sendMail(mailOptions); console.log(`[EmailUtils] Email sent! messageId: ${info.messageId}`); return info; }; export default sendEmail;
